package uddug.com.domain.entities

class ChatListEntity {
    var nameContact: Int
    var textMessage: Int

    constructor(nameContact: Int, textMessage: Int) {
        this.nameContact = nameContact
        this.textMessage = textMessage
    }
}